# Copie este arquivo para ".env" e preencha com os valores do seu projeto.
# Nunca coloque o arquivo ".env" (com os valores reais) no Git — ele já
# está listado no .gitignore por segurança.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Observação: a busca de empresas via Google Maps (recurso da versão
# anterior, em HTML único) ainda não foi portada para esta versão React.
# Se for adicionada no futuro, a chave entra aqui como VITE_GOOGLE_MAPS_API_KEY.
